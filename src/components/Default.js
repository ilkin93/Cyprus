import React, { Component } from 'react';

export class Default extends Component {
    render() {
        return (
            <div>
                default page
            </div>
        );
    }
}

export default Default;
